import matplotlib.pyplot as plt
import numpy as np

bool = np.loadtxt("booltime.txt")
rac = np.loadtxt("ractime.txt")

for i in range(len(bool)):
	bool[i] = np.log10(bool[i][0])
	rac[i] = np.log10(rac[i])

bool = bool[1:] + bool[:1]
rac = rac[1:] + rac[:1]
x = [10,4,5,6,7,8,9]
plt.plot(x, bool[:, 1])
plt.errorbar(x, bool, 
